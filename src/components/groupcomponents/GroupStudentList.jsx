import React from 'react'

const GroupStudentList = (props) => {
  return (
    <body>
        <div>{studentId}</div>
        <div>{studentName}</div>
        <div>{level}</div>        
        <div>{notes}</div>        
    </body>
  )
}

export default GroupStudentList